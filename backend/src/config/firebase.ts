/**
 * @deprecated Firebase has been replaced with Supabase.
 * Import from '../config/supabase' instead.
 *
 * This file is kept as a placeholder to avoid broken imports during migration.
 * It will be removed once all references are cleaned up.
 */
export { supabase as db, supabase } from './supabase';
export default {};
